# No third-party runtime dependencies. Uses the Python standard library and
# the IBM ADK's tool decorator supplied by the Orchestrate runtime.
# Keep separate from the project CLI/development requirements.txt.
