# Generated by `make openapi`. Render the server placeholder before import.
openapi: 3.0.3
info:
  title: Shipping Eligibility POC
  description: Deterministic dummy shipping eligibility. No real rates, bookings,
    or customer data.
  version: 1.0.0
paths:
  /eligibility:
    get:
      summary: Evaluate simulated shipping rate eligibility
      description: Return a simulated plan tier and discount from confirmed monthly
        shipment volume, destination mix, and business type. All three inputs are
        required. Never invent missing values or use this tool for general shipping
        questions. HTTP 422 means invalid input, not a Standard-tier decision. Does
        not calculate a full shipping quote.
      operationId: evaluate_rate_eligibility
      parameters:
      - name: monthly_volume
        in: query
        required: true
        schema:
          type: integer
          maximum: 1000000
          minimum: 0
          description: Confirmed number of shipments per month, not per week or year.
            Whole number from 0 to 1000000.
          title: Monthly Volume
        description: Confirmed number of shipments per month, not per week or year.
          Whole number from 0 to 1000000.
      - name: destination_mix
        in: query
        required: true
        schema:
          $ref: '#/components/schemas/DestinationMix'
          description: domestic_only for no international shipments; mixed for some
            international up to 50 percent or an explicitly mixed pattern; international_heavy
            for more than 50 percent international or explicitly mostly/all international.
        description: domestic_only for no international shipments; mixed for some
          international up to 50 percent or an explicitly mixed pattern; international_heavy
          for more than 50 percent international or explicitly mostly/all international.
      - name: business_type
        in: query
        required: true
        schema:
          $ref: '#/components/schemas/BusinessType'
          description: Customer-confirmed business category. Required for audit; does
            not alter v1 discounts.
        description: Customer-confirmed business category. Required for audit; does
          not alter v1 discounts.
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EligibilityResult'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
components:
  schemas:
    BusinessType:
      type: string
      enum:
      - ecommerce
      - retail
      - manufacturing
      - logistics
      - other
      title: BusinessType
    DestinationMix:
      type: string
      enum:
      - domestic_only
      - mixed
      - international_heavy
      title: DestinationMix
    EligibilityRequest:
      properties:
        monthly_volume:
          type: integer
          maximum: 1000000.0
          minimum: 0.0
          title: Monthly Volume
          description: Confirmed number of shipments per month, not per week or year.
            Whole number from 0 to 1000000.
        destination_mix:
          $ref: '#/components/schemas/DestinationMix'
          description: domestic_only for no international shipments; mixed for some
            international up to 50 percent or an explicitly mixed pattern; international_heavy
            for more than 50 percent international or explicitly mostly/all international.
        business_type:
          $ref: '#/components/schemas/BusinessType'
          description: Customer-confirmed business category. Required for audit; does
            not alter v1 discounts.
      additionalProperties: false
      type: object
      required:
      - monthly_volume
      - destination_mix
      - business_type
      title: EligibilityRequest
    EligibilityResult:
      properties:
        plan_tier:
          type: string
          enum:
          - Platinum
          - Gold
          - Silver
          - Bronze
          - Standard
          title: Plan Tier
        discount_rate:
          type: number
          maximum: 1.0
          minimum: 0.0
          title: Discount Rate
          description: Fraction, e.g. 0.18 means 18 percent.
        discount_percent:
          type: integer
          maximum: 100.0
          minimum: 0.0
          title: Discount Percent
        eligible_for_discount:
          type: boolean
          title: Eligible For Discount
        inputs:
          $ref: '#/components/schemas/EligibilityRequest'
        rule_id:
          type: string
          title: Rule Id
        reason:
          type: string
          title: Reason
        policy_version:
          type: string
          title: Policy Version
        decision_id:
          type: string
          title: Decision Id
          description: Deterministic reference, not an authentication signature.
        source:
          type: string
          title: Source
          enum:
          - dummy_shipping_eligibility_api
        is_simulated:
          type: boolean
          title: Is Simulated
          enum:
          - true
        limitations:
          items:
            type: string
          type: array
          title: Limitations
      additionalProperties: false
      type: object
      required:
      - plan_tier
      - discount_rate
      - discount_percent
      - eligible_for_discount
      - inputs
      - rule_id
      - reason
      - policy_version
      - decision_id
      - source
      - is_simulated
      - limitations
      title: EligibilityResult
    HTTPValidationError:
      properties:
        detail:
          items:
            $ref: '#/components/schemas/ValidationError'
          type: array
          title: Detail
      type: object
      title: HTTPValidationError
    ValidationError:
      properties:
        loc:
          items:
            anyOf:
            - type: string
            - type: integer
          type: array
          title: Location
        msg:
          type: string
          title: Message
        type:
          type: string
          title: Error Type
        input:
          title: Input
        ctx:
          type: object
          title: Context
      type: object
      required:
      - loc
      - msg
      - type
      title: ValidationError
servers:
- url: ${SHIPPING_API_URL}
